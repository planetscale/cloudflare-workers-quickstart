{
  "name": "pscale-demo",
  "version": "0.0.0",
  "devDependencies": {
    "wrangler": "2.20.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "@planetscale/database": "^1.7.0"
  }
}
